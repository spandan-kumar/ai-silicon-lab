# Laboratory infrastructure

The executable wrappers in this directory are immutable after setup. They
delegate to the protected evaluator/reference implementation and write only
to the mutable run/cache areas described in the root documentation.

